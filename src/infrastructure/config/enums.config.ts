export const enum Secrets {
  JWT = 'secretKey',
}

export const enum Grpc_secrets {
  NAME = 'USER_PACKAGE',
  BASE_PATH = '../../controllers/grpc/',
  PACKAGE = 'users',
  PROTO_PATH = 'proto/auth.proto',
}

export const enum Repository_key {
  USER = 'UserRepository',
  PRODUCT = 'ProductRepository',
  ORDER = 'OrderRepository',
}
